# Configuración de accesos (opcional)
# Permitir conexiones locales y desde la red del contenedor

host    all             all             0.0.0.0/0               md5
host    all             all             ::/0                    md5
