# Configuración avanzada (opcional)
# Ajustes básicos de PostgreSQL
listen_addresses = '*'
max_connections = 100
shared_buffers = 128MB
work_mem = 4MB
maintenance_work_mem = 64MB
effective_cache_size = 512MB
